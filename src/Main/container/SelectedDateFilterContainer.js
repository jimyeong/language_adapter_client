import React from 'react';
import styled from 'styled-components';

const SelectedDateFilterContainerBlock = styled.div``;

function SelectedDateFilterContainer({ children }) {
    return (
        <SelectedDateFilterContainerBlock></SelectedDateFilterContainerBlock>
    );
}

export default SelectedDateFilterContainer;
