const prefix = '/my-calendar';
const routes = {
    myDetail: `${prefix}/edit`,
    infoDetail: `${prefix}/info`,
    addMySchedule: `${prefix}/add`,
    home: `${prefix}`,
};

export { routes, prefix };
